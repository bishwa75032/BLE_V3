{
  "name": "stm32-ble-controller",
  "version": "1.0.0",
  "description": "STM32 BLE LED Controller - Native Android App",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed - pure HTML'",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor-community/bluetooth-le": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
